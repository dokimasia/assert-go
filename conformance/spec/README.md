# Vendored definition

A copy of the assertion definition, rendered as JSON. Do not edit by
hand: run `make spec-sync` and commit the result.

The copy is committed and embedded, so the conformance tests run with
no network and no sibling checkout.
